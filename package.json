{
  "name": "omefans-worker-backend",
  "version": "1.0.0",
  "description": "Backend for gallery management on Cloudflare Workers",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "vercel dev"
  },
  "dependencies": {
    "hono": "^4.4.6",
    "@vercel/postgres": "^0.9.0"
  },
  "devDependencies": {
  }
}
